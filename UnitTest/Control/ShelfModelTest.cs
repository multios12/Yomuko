namespace UnitTestProject1.Control
{
    using System;
    using System.Collections.Generic;
    using ComicLaunch.Model;
    using ComicLaunch.Model.Shelf;
    using Microsoft.VisualStudio.TestTools.UnitTesting;

    [TestClass]
    public class ShelfModelTest
    {
        [TestMethod]
        public void TestConstructor()
        {
            // ShelfModel model = new ShelfModel();
            // Assert.AreEqual(new List<string>(), model.BaseFolderPaths);
            // Assert.AreEqual(new List<BookmarkModel>(), model.Bookmarks);
            // Assert.AreEqual(new BookList(), model.Books);
        }
    }
}
